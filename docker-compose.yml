version: "3.8"

services:
  web:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "4321:4321"
    command: >
      sh -c "npm install &&
             npm run build &&
             npm run preview -- --host 0.0.0.0 --port 4321"
